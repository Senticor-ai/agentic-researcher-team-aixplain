# aixplain API Configuration
# Note: The aixplain SDK uses TEAM_API_KEY internally, but we use AIXPLAIN_API_KEY
# The config module will automatically set TEAM_API_KEY from AIXPLAIN_API_KEY
AIXPLAIN_API_KEY=your_aixplain_api_key_here

# Backend API Configuration
# Host: 0.0.0.0 = accessible from network, 127.0.0.1 = localhost only
API_HOST=0.0.0.0
# Port: Default 8000, change if port is already in use
API_PORT=8000

# Frontend UI Configuration (for reference)
# Note: Frontend port is configured in vite.config.js or via CLI
# Default: 5173 (Vite default)
# To change: npm run dev -- --port 3000
UI_PORT=5173

# Environment
# Options: development, production
ENVIRONMENT=development

# Database Configuration
# Path to SQLite database file (will be created if it doesn't exist)
# Default: ./data/teams.db
# DB_PATH=./data/teams.db
